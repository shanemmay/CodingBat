public String delDel(String str) {
  if(str.indexOf("del") == 1 ){
    int num = str.indexOf("del");
  str = str.substring(0,num) + str.substring((num + 3) , str.length());
  return str;
  }
  return str;
}
