public String altPairs(String str) {
      String result = "";
		  int i = 0;
		  while(i < str.length()){
			  if(i + 2 < str.length()){
				  result += str.substring(i, i + 2);
			  }else{
				  result += str.substring(i, str.length());
			  }
			  i += 4;
		  }
		  return result;
}
